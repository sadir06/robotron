# Core
openai>=1.0.0
python-dotenv>=1.0.0

# Camera
opencv-python>=4.8.0

# API
fastapi>=0.104.0
uvicorn>=0.24.0

# vLLM (for local low-latency inference — install separately if using)
# pip install vllm
# Run: python -m vllm.entrypoints.openai.api_server --model llava-hf/llava-1.5-7b-hf --port 8000
